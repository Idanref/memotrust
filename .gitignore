# dependencies / build output (rebuildable)
node_modules/
dist/

# secrets
.env
.env.local

# the memory store — business data, lives locally only, never committed
memory/

# local tooling / env config
.claude/
.mcp.json
.DS_Store
